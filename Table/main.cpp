// main.cpp
// Print table that shows square and cubes of 1st 11 whole digits in C++
// Author Crimson Codes (@ICrimsonCodes)


#include <iostream>
using namespace std;

int main()
{
	int a;
	a = 0;
	
cout << "Number\tSquare\tCube\n"
		<< a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout<< a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	a = a + 1;
	cout << a << '\t' << a * a << '\t' << a * a * a << '\n';
	return 0;
}